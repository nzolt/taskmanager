    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <a class="navbar-brand" href="{{ path_for('index', { 'name': 'index' }) }}"><span class="glyphicon glyphicon-home" aria-hidden="true"></span> Main</a>
                <a class="navbar-brand" href="{{ path_for('list', { 'name': 'list' }) }}"><span class="glyphicon glyphicon-list" aria-hidden="true"></span> List tasks</a>
                <a class="navbar-brand" href="{{ path_for('create', { 'name': 'new' }) }}"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Add task</a>
            </div>
        </div><!-- /.container-fluid -->
    </nav>
